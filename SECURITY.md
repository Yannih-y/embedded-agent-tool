# 安全模型（Security）

> 一句话定位：**单机、单用户、本机回环**的共享记忆服务。它信任本机进程，
> 防御来自浏览器与写入内容的攻击面。跨机器暴露不是设计目标。

## 信任边界

| 边界 | 现状 | 依据 |
|------|------|------|
| 网络 | 默认只监听 `127.0.0.1`，不上云、不出网（仅显式配置 LLM key 的固化链路会调云端网关） | `MEMPOOL_HOST` 默认回环 |
| 浏览器 / DNS rebinding | REST 与 `/mcp` 均校验 Host 头，非白名单 403 | `server.py` host_guard + MCP SDK 内建防护 |
| 本机进程 | **无鉴权**——能访问回环端口的本机进程可读写任意 `user_id` 的数据。这是刻意取舍（零设置体验），本机已失守时任何本地鉴权同样失守 | 设计决策 |
| 写入内容 | 写前强制审查：命令式提示注入（中英）、隐形 unicode（零宽/bidi）、高置信凭证格式（sk-/ghp_/AKIA/xox/私钥块）命中即拒 | `content_guard.py`，唯一写入闸口 `pool.add` |
| 检索消费方 | 检索结果会进入各 agent 的上下文——写入面即注入面，故拦截放在写入侧；消费侧 agent 仍应把记忆当**数据**而非指令 | 使用约定 |

## 数据事实

- 全部数据落 `~/.agent_memory_pool/`（faiss 向量 + SQLite），**不加密**——
  磁盘加密请用系统级方案（BitLocker / FileVault）
- 凭证类内容（API key、token、私钥）写入会被拒绝；这是兜底，不是许可清单——
  请不要往池里放任何秘密
- `user_id` 是检索过滤边界，不是安全边界（无鉴权，见上）

## 部署红线

- **不要**把 `MEMPOOL_HOST` 改成 `0.0.0.0` 后暴露在不可信网络——服务无鉴权
- 局域网部署（如 NAS 探活）只用 `MEMPOOL_ALLOWED_HOSTS` 扩 Host 白名单，
  并确保网络层有访问控制（防火墙 / VPN / Tailscale）
- 慢记忆搭档 nestwork 仓库含个人数据，远程必须是**私有**仓库

## 漏洞报告

发现安全问题请开 GitHub issue（不含利用细节），或在 issue 中留联系方式约定
私下沟通渠道。
